<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.LJZ.DAO.AskTest">
    <select id="get_ask_count1" parameterType="Int" resultType="Int">select belong_class_id from ask where ask_id=#{ask_id}</select>

</mapper>